-r requirements.txt
# Development and Code Quality Tools
# Install with: pip install -r requirements-dev.txt

# Code Formatting (ESSENTIAL - Run always)
autopep8==2.3.2
black==26.3.1
isort==5.13.2           # Import sorting (PEP 8 compliant)
docformatter==1.7.8     # Docstring formatting (PEP 257 compliant)

# Linting (ESSENTIAL - Run always)
flake8==7.3.0
ruff==0.15.12
pylint==3.3.9

# Type Checking (OPTIONAL - Run periodically)
mypy==1.20.2

# Testing (ESSENTIAL - Run always)
# Note: pytest is also in requirements.txt (needed for app testing)
pytest==8.4.2
pytest-cov==6.3.0
pytest-mock==3.15.1
pytest-env==1.6.0
pytest-testmon>=2.1.0   # Smart test selection: only re-runs tests affected by code changes
diff-cover>=9.0.0
iniconfig==2.3.0
pandas-stubs==2.3.3.260113
pluggy==1.6.0
types-pytz==2025.2.0.20251108

# Code Quality Analysis (OPTIONAL - Run periodically)
bandit==1.9.4
radon==6.0.1
yamllint==1.38.0        # YAML file validation

# Pre-commit hooks (OPTIONAL - Configure later)
# Run 'pre-commit install' only when ready to use
pre-commit==4.6.0
conventional-pre-commit==4.4.0

# HTML/Template Linting
djlint>=1.36.4
